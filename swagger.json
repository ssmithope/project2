{
  "swagger": "2.0",
  "info": {
    "title": "Project 2 API",
    "version": "1.0.0"
  },
  "host": "localhost:10000",
  "schemes": ["http"],
  "paths": {
    "/users": {
      "get": { "summary": "Get all users", "responses": { "200": { "description": "Success" } } }
    },
    "/users/{id}": {
      "get": { "summary": "Get a user by ID", "responses": { "200": { "description": "Success" } } }
    }
  }
}
